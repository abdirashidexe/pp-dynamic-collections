import React from 'react';
//import { playersData } from '../players';
//import ItemCard from './Card';

export default function mainContent() {
    return(
        <div>
            {/*
            {playersData.map((player, index) => (
                <ItemCard key={index} data={player}/>
            ))}
            */}
        </div>
    )
}